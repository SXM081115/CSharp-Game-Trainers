<UserControl x:Class="Cheaters.Windows.WindowChrome"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="400">
    <Border Style="{DynamicResource WindowTitleBarStyle}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!-- 窗口标题 -->
            <TextBlock x:Name="WindowTitle" 
                       Grid.Column="0" 
                       Text="{Binding Title, RelativeSource={RelativeSource AncestorType=Window}}"
                       Foreground="{StaticResource 通用文本色}"
                       VerticalAlignment="Center"
                       Margin="10,0,0,0"
                       FontWeight="Bold"/>

            <!-- 窗口控制按钮 -->
            <StackPanel Grid.Column="1" 
                        Orientation="Horizontal">
                <!-- 最小化按钮 -->
                <Button x:Name="MinimizeButton"
                        Style="{DynamicResource WindowMinimizeButtonStyle}"
                        Focusable="True"
                        IsTabStop="True"
                        Click="MinimizeButton_Click"
                        Visibility="{Binding CanMinimize, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                
                <!-- 最大化/还原按钮 -->
                <Button x:Name="MaximizeButton"
                        Style="{DynamicResource WindowMaximizeButtonStyle}"
                        Focusable="True"
                        IsTabStop="True"
                        Click="MaximizeButton_Click"
                        Visibility="{Binding CanMaximize, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                
                <!-- 关闭按钮 -->
                <Button x:Name="CloseButton"
                        Style="{DynamicResource WindowCloseButtonStyle}"
                        Content="✕"
                        Focusable="True"
                        IsTabStop="True"
                        Click="CloseButton_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>